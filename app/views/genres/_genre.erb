<div class="newpar">
    <ul class="genres">
    <% @genres.each do |genre| %>
      <ul class="genre-link">
          <li class="genre-link"><a class="part" href="<%= "genres/#{genre.id}" %>"><%= genre.name %></a></li>
        </ul>
      </li>
    <% end %>
</ul>
</div>